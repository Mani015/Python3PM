# list contains allows duplicate values:

a = [1,2,3,4,5]
# print('without duplicate values:',a)
# without duplicate values: [1, 2, 3, 4, 5]

b = [1,2,3,4,5,6,1,2,3]
print('with duplicate values:',b)
# with duplicate values: [1, 2, 3, 4, 5, 6, 1, 2, 3]

# Duplicates: The list can contain duplicates i.e., lists can have two items with the same values.
